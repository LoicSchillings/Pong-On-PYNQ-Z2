#include "game_state.h"

/* Global game state instance */
game_state_t game = {
    .p1 = { .y = (SCREEN_H - PADDLE_H) / 2 },
    .p2 = { .y = (SCREEN_H - PADDLE_H) / 2 },

    .ball = {
        .x  = SCREEN_W / 2,
        .y  = SCREEN_H / 2,
        .vx = 4,
        .vy = 3
    },

    .score_p1 = 0,
    .score_p2 = 0,

    .p1_ready = 0,
    .p2_ready = 0,

    .mode = GAME_WAIT_START
};
